package model;

public class StandardCar extends Car {
    public StandardCar(int id, String brand, String plateNumber, String color, int year) {
        super(id, brand, plateNumber, color, year);
    }
}
